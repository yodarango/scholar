module.exports = {
   images: {
      domains: ["drive.google.com", "res.cloudinary.com", "images.unsplash.com"]
   },
   distDir: "build"

   //swcMinify: true
};

// const nextConfig = {
//    distDir: "build"
// };

// module.exports = nextConfig;
